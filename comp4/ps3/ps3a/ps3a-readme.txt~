/**********************************************************************
 *  N-Body Simulation ps3a-readme.txt template
 **********************************************************************/

Name: Angel Zheondre Calcano 
OS: Ubuntu lts
Machine (e.g., Dell Latitude, MacBook Pro): Sony vaio
Text editor: Emacs
Hours to complete assignment (optional): about 17 

Majority of the time was spent on trying to organize the code in such a way 
that when I add more features it would be plug and play. Time was also spent
in trying to figure out the physics simulation. I just need to find out 
the xcomponent velocity based on the force between a specific planet and 
the sun.   
/**********************************************************************
 *  Briefly explain the workings of the features you implemented.
 *  Include code excerpts.
 **********************************************************************/
My class inherits Drawable, the while loop for the draw method looks like: 
 while (window.isOpen()){
 
    sf::Event event;
    while (window.pollEvent(event)) {
      if (event.type == sf::Event::Closed)
	window.close();
    }
    window.clear();
    for( i = 0 ; i < n ; i++ ) window.draw(*space[i]) ; 
    window.display();
  }

Loading universe from stdin: 

cin >> n ;  cin >> spcrdi ; 

  for( i = 0 ; i < n ; i++ ) { 
    body *planet= new body( spcrdi, 900 ) ; 
    cin >> planet; 
    space.push_back( planet )  ;   
   } 

Overloading the >> operator to read in a row from universe file: 

friend istream &operator >> (istream &input, body *S) { 
    input >> S->xpos >> S->ypos >> S->xvel >> S->yvel >> S->mass >> S->fname ;
    S->setpos() ; 
    S->setImage(); 
    return input ; 
  }

Supports arbitrary # of objects: 

cin >> n ; 

Scaling :

ws = 900 ;

body *planet= new body( spcrdi, ws ) ;  

RenderWindow window( VideoMode( ws, ws), "ps3 AC" ) ;

body::body(  double ss, double ws ) { 
  ceny = cenx = ss/(1e+9) + 200 ;
  winsiz = ws ; 
} 

/**********************************************************************
 *  List whatever help (if any) you received from the instructor,
 *  classmates, or anyone else.
 **********************************************************************/
None

/**********************************************************************
 *  Describe any serious problems you encountered.                    
 **********************************************************************/
Trying to figure out how to find the size of the sprite, how to insert 
cin values directtly in the constructor.  

/**********************************************************************
 *  List any other comments here.                                     
 **********************************************************************/

